import {
    StyleSheet
} from 'react-native'

export default StyleSheet.create({
    post_title: {
        alignSelf: 'center',
        fontSize: 16,
    },
    post_button: {
        borderWidth: 1,
        borderTopWidth: 0,
        borderColor: 'rgba(0,0,0,0.3)',
        padding: 8,
    }
});
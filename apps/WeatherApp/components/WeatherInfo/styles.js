import {
    StyleSheet
} from 'react-native'

export default StyleSheet.create({
    weather_img: {
        width: 50,
        height: 50,
        marginTop: 'auto',
        marginBottom: 'auto',
        alignSelf: 'center',
    }
});
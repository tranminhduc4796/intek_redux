import {
    StyleSheet
} from 'react-native'

export default StyleSheet.create({
    city_input: {
        width: 300,
        height: 40,
        borderColor: '#33bbff',
        borderWidth: 1,
        alignSelf: 'center',
        textAlign: 'center',
        marginTop: 10,
    },
});